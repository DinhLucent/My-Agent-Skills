# ═══════════════════════════════════════════════════════════
# TASK HUB — BACKLOG (Central Task Queue)
# ═══════════════════════════════════════════════════════════
#
# This is the ONLY place to create and distribute tasks to Agents.
# Each task MUST specify assigned_role. Only matching Agent can claim.
#
# Status: todo | claimed | in_progress | in_review | done | blocked
# Priority: critical | high | medium | low
# ═══════════════════════════════════════════════════════════

project: ""          # ← Project name
sprint: 1
created_by: "user"   # user | producer-agent | cto-agent

tasks:
  # ─── Example task (delete when using for real) ───
  # - id: "TASK-001"
  #   title: "Design database schema for Patient"
  #   description: "Create ERD and migration for patients, appointments, records tables"
  #   assigned_role: "backend-agent"       # ← ONLY this agent can claim
  #   priority: "high"
  #   status: "todo"
  #   dependencies: []                     # Task IDs that must be done first
  #   created_at: "2026-03-29T13:30:00"
  #   deadline: "2026-03-31"
  #   acceptance_criteria:
  #     - "Schema has patients table with ≥8 fields"
  #     - "Migration script runs on PostgreSQL"
  #     - "Indexes created for common queries"
  #   output_files: []                     # Agent fills when completed
  #   notes: "See ADR-001 for PostgreSQL decision"
